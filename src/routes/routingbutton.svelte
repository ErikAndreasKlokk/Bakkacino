<!-- <script>
    import { navigate } from 'svelte-routing';
    
    const handleClick = (routing) => {
      navigate(routing);
    };

    export let routing;
</script>
    
<button on:click={handleClick({routing})}>Go to Result Page</button> -->